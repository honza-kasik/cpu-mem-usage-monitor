* Add validation for -p and -c options to not to be used together
* Add validation for -N and -F options too
* Add validation for -d (log file location) option to be valid only with -g (graph enabled)
